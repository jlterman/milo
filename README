From its work directory, a release build is generated by:

     make release

A debug build:

     make debug

This makefile assumes gcc 8.x

Dependencies beyond standared libc and libc++:

boost
ncurses
utfcpp
gtk+3.0

